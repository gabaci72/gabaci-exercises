function printLengthOfText(text) {
    console.log(text.length);
}

printLengthOfText(3); // Prints: undefined (because 3 is not a string)
printLengthOfText("hello"); // Prints: 5 (because "hello" is a string with 5 characters)