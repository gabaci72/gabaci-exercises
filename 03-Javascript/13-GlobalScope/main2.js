//define a function
const logVisibleLightWaves = () => {
  const lightWaves = 'Moonlight';//2.using const, create a variable lightWaves and set it equal to 'Moonlight'
  console.log(lightWaves);//3.add a console.log() statement that will log the value of the lightWaves variable when the function runs.
};

//4.Call the logVisibleLightWaves() function from outside the function
logVisibleLightWaves();

// 5.Attempt to log lightWaves outside the function
// console.log(lightWaves); // This will cause a ReferenceError
